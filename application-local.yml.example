# 复制本文件为项目根目录下的 application-local.yml（该文件名已在 .gitignore 中），再填入真实密钥。
# Spring 会通过 spring.config.import 自动加载 application-local.yml。
dashscope:
  api-key: "在此填写 DashScope API Key"
  # qwen-vl-max 请保持 false，否则百炼会报 thinking_budget 400
  vision-enable-thinking: false
